# Resend API key — required for the contact form to deliver mail.
# Without it, submissions fail with a handled error instead of sending.
RESEND_API_KEY=

# Google Analytics 4 measurement ID. Not a secret — it ships in the page.
# Read at build time, and only used in production builds.
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX
